# Test environment configuration
# Used by run-tests.sh and playwright tests

# API URL - use proxy to avoid CORS issues
PUBLIC_API_URL=/api

# Backend URL for Vite proxy target
API_BACKEND_URL=http://localhost:8080

# Playwright base URL
PLAYWRIGHT_BASE_URL=http://localhost:4321
